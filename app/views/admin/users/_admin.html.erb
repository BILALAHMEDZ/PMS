<% title "-All Users" %>
<div class="container">
  <div>
    <%= form_tag admin_users_path, method: 'get' do %>
      <%= text_field_tag :search, params[:search], class: 'form-control col-11 float-left', placeholder: 'Search By Name'%>
      <%= submit_tag 'Search', class: 'btn btn-primary float-right'%>
    <%end%>
  </div>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Name</th>
        <th>Status</th>
        <th>Type</th>
        <th>Email</th>
        <th>Option</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.name %></td>
          <td><%= user.status %></td>
          <td><%= user.type %></td>
          <td><%= user.email %></td>
          <td>
            <%= link_to '', edit_admin_user_path(user) , class: 'btn btn-primary fa fa-pencil' %>
            <%= link_to '', admin_user_path(user) , method: :delete, data: { confirm: 'Are you sure?'}, class: 'btn btn-danger fa fa-trash-o' , id: 'delete' %></td>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <%= link_to fa_icon('plus-square', text: 'Add User'), new_admin_user_path, class: 'btn btn-primary' %>
  <%= paginate @users %>
</div>
